
const Test = () => {

    return (
        <h1>
            nextjs는 app 폴더 안에 특정 폴더를 만들고 그 폴더 안에 page.tsx 파일을 만들면 자동으로 구조에 맞게 라우팅 된다.<br />
            page.tsx에서 해당 컴포넌트를 출력할 때는 export default를 무조건 해줘야한다.
        </h1>
    );
}

export default Test;